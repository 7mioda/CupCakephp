{% extends "layout.twig" %}
{% block body %}
    <br>    <br>    <br>    <br>    <br>

    <div class="container">
        <h1>{{ item.id }}</h1>
        {{ item.price }}<br>
        {{ item.quantity  }}<br>
        {{ item.name }}<br>
        {{ item.category  }}<br>

    </div>

{% endblock %}